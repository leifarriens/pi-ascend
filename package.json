{
  "name": "pi-ascend",
  "version": "0.1.0",
  "private": true,
  "description": "A bounded model-escalation extension for Pi",
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "smoke": "node --experimental-strip-types scripts/smoke.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm test && npm run smoke && npm run typecheck"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
